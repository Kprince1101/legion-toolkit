## What changed

## Why

## Verified

- [ ] `yarn validate` is green locally
- [ ] Added a changeset (`yarn changeset`) or this change needs no release
- [ ] New rules or checks have tests and a fixture
