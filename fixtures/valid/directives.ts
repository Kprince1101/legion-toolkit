#!/usr/bin/env node
// eslint-disable-next-line no-console -- the CLI prints its own usage
console.log('usage');
/* webpackChunkName: "lazy-panel" */
export const marker = /* @__PURE__ */ Object.freeze({});
